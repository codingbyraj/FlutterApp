class Player {
  String name;
  String fullName;
  String imagePath;
  String description;
  String style;
  Player(
      {this.name, this.fullName, this.imagePath, this.style, this.description});
}
